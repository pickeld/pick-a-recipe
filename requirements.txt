yt-dlp[curl-cffi]
faster_whisper
openai
# Only needed for a provider configured with the Anthropic API type; imported
# lazily so the rest of the app runs without it.
anthropic
pydantic>=2
python-dotenv
requests
flask
flask-socketio
google-genai
authlib
PyJWT
argon2-cffi
beautifulsoup4
# Web Push for job-outcome notifications. Brings py-vapid and cryptography,
# which ui/push.py also uses to mint the VAPID key pair.
pywebpush
